{
  "name": "smart-city-transport",
  "version": "1.0.0",
  "description": "Smart City Public Transport Crowding Indicator",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "cd frontend && npm run build",
    "test:api": "node --test api/*.test.js"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "vercel": "^33.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
